# Valuation Scan Rebuild Archive

This directory is retained as an early implementation/OpenClaw semantic-reconstruction log.

It records the 2026-08-09 calculation specification, source-field mapping, unresolved product decisions, and dated golden fixtures. It is historical provenance for rebuilding and review, not the current public contract, live market data, or a runnable execution adapter.
